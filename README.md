##### 框架设计
版本信息:
- springboot:2.7.18;
- jdk: 1.8.0_381

采用`SpringBoot`框架进行开发，主要包括以下几个模块：
- `controller`：控制层，负责接收请求，调用服务层处理业务逻辑，并返回响应结果。
- `service`：服务层，负责处理业务逻辑，调用数据访问层进行数据操作。
- `service.impl`：服务实现层，实现服务层定义的接口，处理具体的业务逻辑。
- `dao`：数据访问层，负责与数据库进行交互，进行数据的增删改查操作。
